apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gestion-usuarios.fullname" . }}-test-connection"
  labels:
    {{- include "gestion-usuarios.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gestion-usuarios.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

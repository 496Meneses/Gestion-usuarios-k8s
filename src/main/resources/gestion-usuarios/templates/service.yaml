apiVersion: v1
kind: Service
metadata:
  name: {{ include "gestion-usuarios.fullname" . }}
  labels:
    {{- include "gestion-usuarios.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gestion-usuarios.selectorLabels" . | nindent 4 }}
